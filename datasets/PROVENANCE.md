# Dataset provenance

`uphill_push_v1` is the sole retained dataset. Its contract, manifest, split assignment, normalization, and exhaustive replay audit remain inside the dataset directory and `datasets/READINESS.json`. Historical model checkpoints and experiment results were deleted during the E1/E2 reset.
